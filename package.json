{
  "name": "root-products-monorepo",
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install",
    "install-all": "npm install --prefix frontend && npm install --prefix backend"
  }
}